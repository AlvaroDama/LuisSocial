<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
x:Class="LuisSocial.View.Contactos"

xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
mc:Ignorable="d"
             xmlns:viewModel1="clr-namespace:LuisSocial.ViewModel;assembly=LuisSocial"
             xmlns:contactos="clr-namespace:LuisSocial.ViewModel.Contactos;assembly=LuisSocial"
             d:DataContext="{d:DesignInstance contactos:ContactosViewModel}"

Title="{Binding Titulo}">
  
  <StackLayout>
    <StackLayout Orientation="Horizontal">
      <Button Text="+"></Button>
      <Button Text="Recibidos"></Button>
    </StackLayout>

    <ListView ItemsSource="{Binding Contactos, Mode=TwoWay}"
              SelectedItem="{Binding ContactoSeleccionado, Mode=TwoWay}">
      <ListView.ItemTemplate>
        <DataTemplate>
          <ImageCell Text="{Binding Nombre}" Detail="{Binding Apellidos}"></ImageCell>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
    
  </StackLayout>
  
  
</ContentPage>